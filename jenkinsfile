pipeline {
    agent any

    tools {
        nodejs 'NodeJS 18'
    }

    stages {
        stage('Clone Code') {
            steps {
                git branch: 'main', url: 'https://github.com/prakashbhati086/aws.git'
            }
        }

        stage('Install Dependencies') {
            steps {
                dir('express-frontend') {
                    sh 'npm install'
                }
            }
        }

        stage('Restart App') {
            steps {
                dir('express-frontend') {
                    sh 'pm2 restart express-app || pm2 start app.js --name express-app'
                }
            }
        }
    }
}
